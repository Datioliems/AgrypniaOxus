# DrowsyDriver — Streamlit (deploy-safe). Streamlit Community Cloud đọc file này.
# App chạy đầy đủ bằng chế độ CHỤP ẢNH (st.camera_input) — KHÔNG cần WebRTC.

streamlit>=1.35
numpy>=1.26,<2                 # QUAN TRỌNG: mediapipe/opencv hay VỠ với numpy 2.x → ghim <2
pillow>=10.0
pandas>=2.0
opencv-python-headless>=4.8   # headless: hợp cloud (KHÔNG dùng opencv-python để tránh lỗi libGL)
mediapipe>=0.10,<0.11         # FaceLandmarker → EAR/MAR (dòng 0.10.x có wheel ổn định cho Python 3.11/3.12)
streamlit-autorefresh>=0.0.1  # auto-rerun mỗi 1.5s khi đang giám sát → monitoring liên tục

# ─── TÙY CHỌN: camera TRỰC TIẾP (WebRTC). Thiếu vẫn chạy đầy đủ bằng chế độ chụp ảnh. ───
# Hai gói này hay gây LỖI BUILD trên Streamlit Cloud (av cần ffmpeg, webrtc cần TURN server)
# → để mặc định TẮT cho deploy ổn định. Bỏ comment nếu chạy LOCAL và muốn xem camera trực tiếp.
# streamlit-webrtc>=0.47
# av>=11.0
