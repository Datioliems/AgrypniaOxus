# DrowsyDriver — Python dependencies cho LOCAL environment
# Cài đặt: pip install -r requirements_local.txt
#
# Windows: Mở PowerShell tại thư mục DrowsyDriverAndroid/
# Python >= 3.9 required

# ─── Core image processing ────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# ─── Face detection / landmarks ───────────────
mediapipe>=0.10.0         # FaceLandmarker 478 points

# ─── Augmentation ─────────────────────────────
albumentations>=1.3.0     # Nếu v2.x: API GaussNoise thay đổi, xem step6

# ─── Data split ───────────────────────────────
scikit-learn>=1.3.0

# ─── Visualization (EDA) ──────────────────────
matplotlib>=3.7.0

# ─── TFLite verification ──────────────────────
# Chỉ cần cho verify_tflite trong step8 — bỏ nếu máy yếu
tensorflow>=2.13.0        # HOẶC tensorflow-cpu nếu không có GPU

# ─── Training (CNN Eye / Yawn) ────────────────
# Cài trên LOCAL để train CNN (nhỏ, không cần GPU mạnh)
keras>=2.13.0             # Đi kèm tensorflow

# ─── Utilities ────────────────────────────────
tqdm>=4.65.0              # Progress bar
requests>=2.31.0          # Download datasets
roboflow>=1.1.0           # Roboflow API (tải dataset)

# ─── Report / Export ──────────────────────────
pandas>=2.0.0             # Streamlit dashboard
plotly>=5.15.0            # Interactive charts
streamlit>=1.28.0         # Dashboard UI

# ─── KHÔNG cần trên LOCAL (chỉ COLAB) ─────────
# ultralytics    ← YOLO training → COLAB only (cần T4 GPU)
# torch          ← PyTorch → COLAB only
# torchvision    ← COLAB only
